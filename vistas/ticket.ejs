<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="container">
        <div class="glass-card">
            <h1><i class="fas fa-check-circle" style="color: #00e676;"></i> Pago Exitoso</h1>
            <p>Tu viaje está listo.</p>

            <div class="ticket-info">
                <h2>Ruta: <%= ticket.ruta_nombre %></h2>
                <div class="qr-code">
                    <img src="<%= ticket.codigo_qr %>" alt="Código QR del Ticket">
                </div>
                <p style="margin-top: 15px; font-size: 0.9rem; color: #d1c4e9;">Presenta este código en los validadores de la estación.</p>
            </div>

            <div class="detalles-factura" style="text-align: left; font-size: 0.9rem; margin-top: 20px; border-top: 1px solid rgba(255,255,255,0.1); padding-top: 15px;">
                <p><strong>Ticket ID:</strong> #<%= ticket.id %></p>
                <p><strong>Fecha:</strong> <%= new Date(ticket.fecha_compra).toLocaleString() %></p>
                <p><strong>Origen:</strong> <%= ticket.origen %></p>
                <p><strong>Destino:</strong> <%= ticket.destino %></p>
                <p><strong>Total:</strong> $<%= parseFloat(ticket.precio).toFixed(2) %></p>
            </div>

            <a href="/" class="btn-primary" style="background: transparent; border: 1px solid white; margin-top: 30px;">Volver al Inicio</a>
        </div>
    </div>
</body>
</html>
