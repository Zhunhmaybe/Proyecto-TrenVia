<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        .admin-container { padding: 2rem; max-width: 1200px; margin: 0 auto; }
        .financial-summary {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }
        .financial-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.15);
            display: flex;
            align-items: center;
            gap: 1rem;
            min-width: 200px;
            transition: transform 0.3s ease;
        }
        .financial-card:hover {
            transform: translateY(-3px);
        }
        .financial-card i { font-size: 2rem; opacity: 0.9; }
        .financial-card div { display: flex; flex-direction: column; }
        .financial-card h3 { margin: 0; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.5px; opacity: 0.9; }
        .financial-card .stat-number { font-size: 1.5rem; font-weight: bold; margin: 0; }
        table { width: 100%; border-collapse: collapse; margin-top: 1rem; background: rgba(255, 255, 255, 0.227); }
        th, td { padding: 1rem; text-align: left; border-bottom: 1px solid #ddd; }
        th { background: #f4f4f429; }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="glass-card">
        <h1>Reporte de Tickets Vendidos</h1>
        
        <div class="financial-summary">
            <div class="financial-card">
                <i class="fas fa-ticket-alt"></i>
                <div>
                    <h3>Tickets Vendidos</h3>
                    <p class="stat-number"><%= stats ? stats.tickets : 0 %></p>
                </div>
            </div>
            <div class="financial-card">
                <i class="fas fa-dollar-sign"></i>
                <div>
                    <h3>Ingresos Totales</h3>
                    <p class="stat-number">$<%= stats && stats.ingresos ? parseFloat(stats.ingresos).toFixed(2) : '0.00' %></p>
                </div>
            </div>
        </div>
        <table class="glass-table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Usuario</th>
                    <th>Ruta</th>
                    <th>Estado</th>
                    <th>Fecha</th>
                </tr>
            </thead>
            <tbody>
                <% tickets.forEach(ticket => { %>
                <tr>
                    <td><%= ticket.id %></td>
                    <td><%= ticket.usuario_nombre %></td>
                    <td><%= ticket.ruta_nombre %></td>
                    <td>
                        <span style="padding: 4px 8px; border-radius: 4px; background: <%= ticket.estado === 'pagado' ? '#d4edda' : '#f8d7da' %>; color: <%= ticket.estado === 'pagado' ? '#155724' : '#721c24' %>;">
                            <%= ticket.estado %>
                        </span>
                    </td>
                    <td><%= ticket.fecha_compra ? ticket.fecha_compra.toLocaleString() : 'N/A' %></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </div>
</body>
</html>
