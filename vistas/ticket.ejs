<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar') %>
    <div class="ticket-container">
        <div class="invoice-card">
            <div class="invoice-header">
                <div class="invoice-logo">
                    <i class="fas fa-train"></i> Metro
                </div>
                <div class="invoice-title">Comprobante de Viaje</div>
                
                <div class="success-icon">
                    <i class="fas fa-check-circle"></i>
                </div>
                <div class="success-message">Pago Exitoso</div>
            </div>

            <div class="invoice-body">
                <div class="invoice-row">
                    <span class="invoice-label">Ticket ID:</span>
                    <span class="invoice-value">#<%= ticket.id %></span>
                </div>
                <div class="invoice-row">
                    <span class="invoice-label">Fecha:</span>
                    <span class="invoice-value"><%= new Date(ticket.fecha_compra).toLocaleString() %></span>
                </div>
                
                <div style="margin: 15px 0; border-bottom: 2px dashed #eee;"></div>

                <div class="invoice-row">
                    <span class="invoice-label">Ruta:</span>
                    <span class="invoice-value"><%= ticket.ruta_nombre %></span>
                </div>
                <div class="invoice-row">
                    <span class="invoice-label">Origen:</span>
                    <span class="invoice-value"><%= ticket.origen %></span>
                </div>
                <div class="invoice-row">
                    <span class="invoice-label">Destino:</span>
                    <span class="invoice-value"><%= ticket.destino %></span>
                </div>
            </div>

            <div class="qr-section">
                <img src="<%= ticket.codigo_qr %>" alt="Código QR del Ticket">
                <p class="qr-help">Escanea este código en el validador</p>
            </div>

            <div class="invoice-total">
                <span class="total-label">Total Pagado:</span>
                <span class="total-amount">$<%= parseFloat(ticket.precio).toFixed(2) %></span>
            </div>

            <div class="invoice-footer">
                <a href="/" class="btn-invoice">Volver al Inicio</a>
                <p style="margin-top: 15px; font-size: 0.75rem; color: #aaa;">Gracias por viajar con nosotros</p>
            </div>
        </div>
    </div>
</body>
</html>
