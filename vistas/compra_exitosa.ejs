<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/estilos.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>
<body>
    <%- include('partials/navbar') %>

    <div class="container" style="padding-top: 40px;">
        <div class="glass-card" style="text-align: center; max-width: 800px; margin: 0 auto;">
            <div style="font-size: 4rem; color: #2ecc71; margin-bottom: 20px;">
                <i class="fas fa-check-circle"></i>
            </div>
            <h1 style="color: white; margin-bottom: 30px;">¡Compra Exitosa!</h1>
            
            <div class="ticket-summary-card" style="background: rgba(255, 255, 255, 0.1); border-radius: 15px; padding: 20px; margin-bottom: 30px;">
                <div style="display: flex; justify-content: space-around; flex-wrap: wrap;">
                    <div class="summary-item">
                        <small>Cantidad de Tickets</small>
                        <p style="font-size: 1.5rem; font-weight: bold;"><%= cantidad %></p>
                    </div>
                    <div class="summary-item">
                        <small>Total Pagado</small>
                        <p style="font-size: 1.5rem; font-weight: bold; color: #2ecc71;">$<%= total %></p>
                    </div>
                </div>
            </div>

            <h3 style="text-align: left; margin-bottom: 20px;"><i class="fas fa-ticket-alt"></i> Tus Tickets</h3>

            <div class="tickets-list" style="display: grid; gap: 20px;">
                <% tickets.forEach((ticket, index) => { %>
                    <div class="glass-card" style="display: flex; justify-content: space-between; align-items: center; border-left: 5px solid var(--accent-color); padding: 15px;">
                        <div style="text-align: left;">
                            <h4 style="margin: 0;">Ticket #<%= index + 1 %></h4>
                            <p style="margin: 5px 0 0 0; font-size: 0.9rem; color: #ddd;">ID: <%= ticket.id %></p>
                            <p style="margin: 5px 0 0 0; font-size: 0.9rem;"><%= ticket.ruta_nombre %></p>
                        </div>
                        <div>
                            <a href="/ticket/<%= ticket.id %>" target="_blank" class="btn-primary" style="padding: 10px 15px; font-size: 0.9rem; background-color: #e74c3c;">
                                <i class="fas fa-file-pdf"></i> Descargar
                            </a>
                        </div>
                    </div>
                <% }); %>
            </div>

            <div style="margin-top: 40px;">
                <a href="/" class="btn-secondary">Volver al Inicio</a>
            </div>
        </div>
    </div>
</body>
</html>
